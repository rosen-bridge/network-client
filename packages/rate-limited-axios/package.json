{
  "name": "@rosen-clients/rate-limited-axios",
  "version": "0.1.1",
  "description": "A lightweight rate-limiting wrapped Axios that helps control the number of HTTP requests and prevent API throttling.",
  "license": "MIT",
  "author": "Rosen Team",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "coverage": "npm run test -- --coverage",
    "lint": "eslint --fix . && npm run prettify",
    "prettify": "prettier --ignore-unknown --write lib/RateLimitedAxios.ts",
    "release": "npm run test -- --run && npm run build && npm publish --access public",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.18.0",
    "@vitest/coverage-istanbul": "^2.0.5",
    "eslint": "^8.56.0",
    "extensionless": "^1.9.6",
    "typescript": "^5.3.3",
    "vitest": "^2.0.5"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "dependencies": {
    "@rosen-bridge/abstract-logger": "^2.0.1",
    "await-semaphore": "^0.1.3",
    "axios": "^1.11.0"
  }
}
